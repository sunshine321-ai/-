# 前端目录说明

```text
public/       不参与打包的静态资源与过渡页面
src/api/     按业务模块调用 Java 后端的接口
src/assets/  样式、图片等前端资源
src/components/  可复用组件
src/config/  接口地址等前端配置
src/router/  Vue Router 路由
src/views/   路由页面
src/App.vue  根组件
src/main.js  前端入口
```

启动：

```powershell
npm install
npm run dev
```
